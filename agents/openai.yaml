interface:
  display_name: "Intake Governance"
  short_description: "Route complex multi-agent work through intake-first governance"
  default_prompt: "Use $intake-governance to triage this request through intake, classify the workflow, and produce the correct governance handoff."

policy:
  allow_implicit_invocation: true
